using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Installer : MonoBehaviour
{
    [SerializeField] bool _canAdjustHeight; // by Wheel
    [SerializeField] bool _canAdjustRotationX; // by W/S
    [SerializeField] bool _canAdjustRotationY; // by A/D
    [SerializeField] GameObject _installObject;
    float _height;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
