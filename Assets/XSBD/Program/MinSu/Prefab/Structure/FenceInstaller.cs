using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[ExecuteInEditMode]
public class FenceInstaller : MonoBehaviour
{
    bool _check;
    float _angle = -120;
    [SerializeField] Transform _rotator;
    [SerializeField] Transform _installer;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void Rotate(Transform rotator, ref float degree)
    {
        
    }

    void Install()
    {

    }
}
